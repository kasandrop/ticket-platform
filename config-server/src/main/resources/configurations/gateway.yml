server:
  port: 8060

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/
cloud:
  gateway:
    discovery:
      locator:
        enabled: true

logging:
  level:
    org.springframework.security: TRACE
    web: DEBUG
